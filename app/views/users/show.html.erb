<h1><%= @user.name %></h1>

<div>
  <%= image_tag user_avatar(@user), alt: '[Фоточка]' %>
</div>

<h1>@<%= @user.username %></h1>

<a href="#" class="button button-green">Задать вопрос</a>

<% @questions.each do |question| %>
    <%= render partial: 'questions/question', object: question %>
<% end %>